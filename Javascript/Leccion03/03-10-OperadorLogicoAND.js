//En javascript la representación de (AND) es (&&) y para que se cumpla ambos resultados deben ser verdaderos
let a = 5;
let valMin = 0,
    valMax = 10;

if (a >= valMin && a <= valMax) {
    console.log("Dentro de rango.");
} else {
    console.log("Fuera de rango.");
}

a = -1,
    valMin = 0,
    valMax = 10;

if (a >= valMin && a <= valMax) {
    console.log("Dentro de rango.");
} else {
    console.log("Fuera de rango.");
}

a = 15,
    valMin = 0,
    valMax = 10;

if (a >= valMin && a <= valMax) {
    console.log("Dentro de rango.");
} else {
    console.log("Fuera de rango.");
}

a = 10,
    valMin = 0,
    valMax = 10;

if (a >= valMin && a <= valMax) {
    console.log("Dentro de rango.");
} else {
    console.log("Fuera de rango.");
}

a = 0,
    valMin = 0,
    valMax = 10;

if (a >= valMin && a <= valMax) {
    console.log("Dentro de rango.");
} else {
    console.log("Fuera de rango.");
}